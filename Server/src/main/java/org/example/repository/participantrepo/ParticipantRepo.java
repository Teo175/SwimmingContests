package org.example.repository.participantrepo;


import org.example.domain.Participant;
import org.example.repository.Repository;

public interface ParticipantRepo extends Repository<Long, Participant> {
}
